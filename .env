# ============================================
# BASE .env (FALLBACK ONLY)
# ============================================
# This file is loaded ONLY if .env.{NODE_ENV} does not exist.
# For actual development/production, use:
#   .env.development  -> npm run dev
#   .env.production   -> NODE_ENV=production npm start
# ============================================

# Default to development mode
NODE_ENV=production
PORT=5000
